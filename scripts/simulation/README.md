# Dossier `scripts/simulation`

Simulator de recharges et CLI.

Fichiers:
- `recharge_simulator.py` : module principal (classes `RechargeSimulator`, `RechargeInput`)
- `cli_simulator.py` : interface en ligne de commande
- Tests associés dans `tests/unit/`

Voir `docs/SIMULATION.md` pour exemples et bonnes pratiques.
