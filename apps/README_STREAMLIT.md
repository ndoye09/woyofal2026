**Woyofal Streamlit App**

Run the minimal Streamlit dashboard to explore the DWH.

Installation (from project root):

```powershell
pip install -r requirements.txt
```

Run:

```powershell
streamlit run apps/streamlit_app.py
```

Default connection values point to the local Docker Postgres used by the project.
