# Dossier `data`

Contient les jeux de données utilisés par le projet.

Structure:
- `raw/` : données sources CSV (ne pas modifier)
- `processed/` : données nettoyées / transformées
- `output/` : exports, rapports et résultats

Ne pas committer de données sensibles ou volumineuses dans le dépôt principal.
