# Dossier `data/raw`

Contient les fichiers sources d'entrée au format CSV utilisés pour les tests et l'ingestion.

Fichiers typiques:
- `zones_senegal.csv`, `users.csv`, `consumption_daily.csv`, `recharges.csv`

Ne pas modifier ces fichiers directement si vous voulez reproduire les importations utilisées en CI.
