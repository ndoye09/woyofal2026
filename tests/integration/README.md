# Dossier `tests/integration`

Tests d'intégration et de cohérence des données (peuvent nécessiter accès DB ou CSV complets).

Conseil : exécuter ces tests après avoir chargé les CSV via `sql/02_load_csvs.sql`.

```
pytest tests/integration/ -q
```
