# Dossier `tests`

Contient suites de tests unitaires et d'intégration exécutable via `pytest`.

Structure:
- `tests/unit/` : tests unitaires rapides
- `tests/integration/` : tests plus lents vérifiant la cohérence des données et intégration DB

Exécution :
```
pytest tests/ -v
```
