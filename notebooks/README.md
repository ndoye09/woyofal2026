# Dossier `notebooks`

Notebooks Jupyter pour l'analyse exploratoire (EDA). Certains notebooks sont convertis en scripts (`scripts/run_eda.py`).

Pour exécuter localement : ouvrir le notebook avec Jupyter Lab/Notebook.
